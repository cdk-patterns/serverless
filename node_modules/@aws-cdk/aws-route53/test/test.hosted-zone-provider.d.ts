import { Test } from 'nodeunit';
declare const _default: {
    'Hosted Zone Provider': {
        'HostedZoneProvider will return context values if available'(test: Test): void;
        'HostedZoneProvider will return context values if available when using plain hosted zone id'(test: Test): void;
    };
};
export = _default;
