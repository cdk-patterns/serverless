import { Test } from 'nodeunit';
declare const _default: {
    'default setup'(test: Test): void;
    'targets can be added using addTargets'(test: Test): void;
    'validation error if vpc link is created and no targets are added'(test: Test): void;
};
export = _default;
